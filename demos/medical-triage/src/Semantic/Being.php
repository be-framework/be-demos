<?php

declare(strict_types=1);

namespace Be\Demo\MedicalTriage\Semantic;

/**
 * Being - branching discriminator
 *
 * Declared as an empty stub so the Be Framework's semantic validator can
 * resolve the `$being` parameter without emitting a "not registered in
 * ontology namespace" notice. The discriminator is an internal framework
 * slot, not user input, so it needs no validation rules.
 *
 * The canonical BeGreeting example in the framework does the same.
 */
final class Being
{
}
